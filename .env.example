# Optional overrides for the Step 4 voice demo.
# Defaults are baked into src/config/vapi.ts so the demo works without this file.
VITE_VAPI_PUBLIC_KEY=
VITE_VAPI_ASSISTANT_ID=

# Calendly inline embed (event URL). Prefill: name, email, phone (location).
VITE_CALENDLY_EMBED_URL=https://calendly.com/peterdankov66/30min
